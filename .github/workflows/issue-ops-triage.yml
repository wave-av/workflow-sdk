name: issue-ops-triage
on:
  issues:
    types: [opened, edited]
permissions:
  contents: read
  issues: write
concurrency:
  group: issue-ops-triage-${{ github.event.issue.number }}
  cancel-in-progress: true
jobs:
  triage:
    if: vars.IO_ENABLED == 'true'
    uses: wave-av/wave-foundation/.github/workflows/io-triage.yml@b8375ceb4875a0a5bb86853e18159185a4d7053a
    permissions:
      contents: read
      issues: write
    with:
      scripts_repo: wave-av/wave-foundation
      scripts_ref: "b8375ceb4875a0a5bb86853e18159185a4d7053a"
      owner: ${{ vars.IO_OWNER }}
    secrets:
      gh_pat: ${{ secrets.ISSUE_OPS_PAT }}
      bot_app_id: ${{ secrets.BOT_ISSUE_OPS_APP_ID }}
      bot_private_key: ${{ secrets.BOT_ISSUE_OPS_PRIVATE_KEY }}
